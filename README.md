These are just supplemental modules to my main autoexec repository. Only use one at a time. 
The autoexec.cfg file (found at main repo) is set to automatically detect and execute one of these networking scripts. Zero configuration is required from the user. 
